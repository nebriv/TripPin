node_modules/
.cache/
__pycache__/

# The deck and everything derived from it. This repo is code only: the live
# stays sit in the Worker's KV namespace and the deployed page reads them from
# the gated API, so nothing here needs them. Keep your local copy on disk --
# the tools read it from there, not from git.
src/stays.js
build/
design/
data/*-trips.json
data/cache/
data/geo/

# Working documents: session reports and the design brief. They name the crew
# and the towns throughout, which is the one thing this repo does not carry.
docs/overnight-brief.md
docs/overnight-report.md
docs/design-brief.md
docs/design-brief.html
